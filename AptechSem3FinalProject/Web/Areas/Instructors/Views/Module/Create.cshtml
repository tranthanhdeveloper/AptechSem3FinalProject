@model Web.Areas.Instructors.Models.ModuleItemViewModel

<div class="box box-info" id="module_@Model.Id">
    <div class="box-header with-border">
        <h3 class="box-title">@Model.Name</h3>
        <h3 class="box-title">
            <button type="button" class="btn btn-box-tool edit-module-title" data-id="@Model.Id" data-title="@Model.Name">
                <i class="fa fa-edit fa-2x"></i>
            </button>
        </h3>

        <div class="box-tools pull-right">
            <button type="button" class="btn btn-box-tool" data-widget="collapse">
                <i class="fa fa-minus"></i>
            </button>
        </div>
    </div>
    <div class="box-body">
        <div class="table-responsive">
            @if (Model.LessonItemViewModels.Count >=0 )
            {
                <table class="table no-margin">
                    <thead>
                        <tr>
                            <th>Lesson title</th>
                            <th>Creation date</th>
                            <th>Is Preview</th>
                            <th>Disable</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var lesson in Model.LessonItemViewModels)
                        {
                            @Html.Partial("../Lesson/Create", lesson)
                        }
                    </tbody>
                </table>
            }
            else
            {
                <div>No lesson found</div>
            }
        </div>
    </div>

    <div class="box-footer clearfix">
        <button type="button" class="btn btn-default add-lesson-modal" data-toggle="modal" data-target="#add-lesson-modal" data-backdrop="static" data-keyboard="true" data-module-id="@Model.Id">Create new lesson</button>
</div>
</div>